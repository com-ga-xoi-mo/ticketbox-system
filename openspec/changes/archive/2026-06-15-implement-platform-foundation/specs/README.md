# Specs Delta

No specs delta is included for this change.

The platform foundation work implements existing target behavior from:

- `openspec/specs/project-governance/spec.md`
- `openspec/specs/submission-readiness/spec.md`

Accepted target behavior does not change, so no `ADDED`, `MODIFIED`, `REMOVED`, or `RENAMED` requirements are defined here.
